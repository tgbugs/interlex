#!/usr/bin/env sh
gunicorn \
    --bind unix:/run/ilxalt/socket \
    --pid /run/ilxalt/pid \
    --name interlex_alt \
    --workers 2 \
    --worker-class gevent \
    --timeout 30 \
    --log-level INFO \
    --capture-output \
    --preload \
    interlex.alt_server:app
